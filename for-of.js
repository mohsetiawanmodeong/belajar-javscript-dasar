const names = ["Wawan", "Setiawan", "Modeong"];
for (const name of names) {
    console.info(`${name}`);
}

const fullName = "Moh. Setiawan Modeong";
for (const character of fullName) {
    console.info(`${character}`);
}

const person = {
    firstName: "Setiawan",
    lastName: "Modeong",
};
for (const name of person) {
    console.info(`${name}`);
}